#include <stdio.h>
#include <string.h>

int main()
{
    char str[100], rev[100];
    int i, length;

    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    length = strlen(str) - 1;   // ignore newline character

    for (i = 0; i < length; i++)
    {
        rev[i] = str[length - 1 - i];
    }
    rev[i] = '\0';

    printf("Reversed string: %s\n", rev);

    return 0;
}
o/p:
Enter a string: World
Reversed string: dlroW
