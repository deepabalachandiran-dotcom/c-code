#include <stdio.h>

// structure to store course details
struct Course
{
    int courseId;
    char courseName[50];
    char instructor[50];
    float fee;
};

int main()
{
    struct Course c;

    // input course details
    printf("Enter Course ID: ");
    scanf("%d", &c.courseId);

    printf("Enter Course Name: ");
    scanf(" %[^\n]", c.courseName);

    printf("Enter Instructor Name: ");
    scanf(" %[^\n]", c.instructor);

    printf("Enter Course Fee: ");
    scanf("%f", &c.fee);

    // display course details
    printf("\n--- Course Details ---\n");
    printf("Course ID   : %d\n", c.courseId);
    printf("Course Name : %s\n", c.courseName);
    printf("Instructor  : %s\n", c.instructor);
    printf("Fee         : %.2f\n", c.fee);

    return 0;
}
o/p:
Enter Course ID: 324
Enter Course Name: c programing
Enter Instructor Name: ravi
Enter Course Fee: 630

--- Course Details ---
Course ID   : 324
Course Name : c programing
Instructor  : ravi
Fee         : 630.00

