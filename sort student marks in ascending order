#include <stdio.h>

int main()
{
    int n, i, j, temp;
    int marks[100];

    printf("Enter number of students: ");
    scanf("%d", &n);

    // Input marks
    printf("Enter marks of students:\n");
    for(i = 0; i < n; i++)
    {
        printf("Student %d: ", i + 1);
        scanf("%d", &marks[i]);
    }

    // Bubble sort to sort marks in ascending order
    for(i = 0; i < n - 1; i++)
    {
        for(j = 0; j < n - i - 1; j++)
        {
            if(marks[j] > marks[j + 1])
            {
                temp = marks[j];
                marks[j] = marks[j + 1];
                marks[j + 1] = temp;
            }
        }
    }

    // Display sorted marks
    printf("\nSorted marks of students (ascending order):\n");
    for(i = 0; i < n; i++)
    {
        printf("Student %d: %d\n", i + 1, marks[i]);
    }

    return 0;
}
o/p:
Enter number of students: 5
Enter marks of students:
Student 1: 80
Student 2: 50
Student 3: 30
Student 4: 55
Student 5: 60

Sorted marks of students (ascending order):
Student 1: 30
Student 2: 50
Student 3: 55
Student 4: 60
Student 5: 80
